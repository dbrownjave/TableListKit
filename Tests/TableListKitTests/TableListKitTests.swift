import XCTest
@testable import TableListKit

final class TableListKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TableListKit().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
